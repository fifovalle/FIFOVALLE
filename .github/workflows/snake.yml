name: Generate Snake Animation

on:
  schedule: # execute every 12 hours
    - cron: "* */12 * * *"
  workflow_dispatch:
  push:
    branches:
    - master

jobs:
  generate:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Generate snake.svg with dynamic colors
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: dist/snake.svg?palette=github-light&dynamic=true

      - name: Add custom animation and style to snake.svg
        run: |
          # You can run a script to edit or add custom styles/animations to the generated SVG
          sed -i 's/#000000/#3498db/g' dist/snake.svg # Example: Change color to blue
          sed -i '/<svg>/a <style> .snake { animation: moveSnake 5s infinite; } @keyframes moveSnake { 0% { transform: translateX(0); } 100% { transform: translateX(100%); } } </style>' dist/snake.svg

      - name: Push snake.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
